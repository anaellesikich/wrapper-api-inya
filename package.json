{
  "name": "web_api",
  "version": "1.0.0",
  "description": "FastAPI wrapper around a pluggable GPT client (Echo default, OpenAI optional), with simple API key auth and optional SSE streaming.",
  "main": "assistants_check.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "openai": "^6.3.0"
  }
}
